# fired via /trigger ScdiPlayerMenu (any player, no op/allow_dummy_trigger
# gate needed - unlike the dummy menu, this only ever touches the
# triggering player's OWN scores, never anyone/anything else's).
function scdi:player_menu_show
